const assert=require('node:assert/strict'),path=require('node:path');
const {pathToFileURL}=require('node:url');
const {chromium}=require(process.env.OMNI_PLAYWRIGHT_PATH||'C:/Users/13758/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const root=path.resolve('build/story-bible');
(async()=>{const browser=await chromium.launch({headless:true,channel:'msedge'});try{
 const page=await browser.newPage({viewport:{width:1500,height:1000}}),errors=[];
 page.on('pageerror',e=>errors.push(e.message));
 await page.goto(pathToFileURL(path.join(root,'index.html')).href);
 assert.equal(await page.locator('.map').count(),10);
 assert(await page.locator('img').evaluateAll(images=>images.every(i=>i.complete&&i.naturalWidth>0)),'All ten actual map images must load');
 await page.screenshot({path:path.join(root,'world-atlas.png'),fullPage:true});
 await page.getByRole('link',{name:'放大关都地图'}).click();assert(page.url().endsWith('map-kanto.jpg'));await page.goBack();
 for(const width of [820,390,320]){await page.setViewportSize({width,height:900});assert(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth+1));}
 await page.goto(pathToFileURL(path.join(root,'characters.html')).href);
 await page.getByLabel('查找姓名或职责').fill('坂木');assert.equal(await page.locator('.person-link:visible').count(),6); // Includes Silver and the elder/keeper whose roles mention Giovanni.
 await page.getByLabel('查找姓名或职责').fill('无此人物');await page.getByText('没有匹配的角色。可以清空关键词或选择全部状态。').waitFor();
 await page.getByLabel('查找姓名或职责').fill('');await page.getByLabel('创作状态').selectOption('candidate');assert.equal(await page.locator('.person-link:visible').count(),38);
 await page.getByLabel('创作状态').selectOption('');await page.getByLabel('查找姓名或职责').fill('亚当');
 await page.getByRole('link',{name:'亚当 已采用 丰缘迁居关都的水战专家；天王',exact:true}).click();
 await page.getByRole('heading',{name:'已写生平',exact:true}).waitFor();assert(await page.locator('body').innerText().then(s=>s.includes('1943 年 5 月 15 日')));
 assert(await page.locator('td[aria-label="尚未创作"]').count()>0,'Unwritten life stages remain blank');
 assert(await page.locator('img').evaluateAll(images=>images.every(i=>i.complete&&i.naturalWidth>0)));
 await page.setViewportSize({width:1280,height:960});await page.screenshot({path:path.join(root,'juan-dossier.png'),fullPage:true});
 await page.goto(pathToFileURL(path.join(root,'people/red.html')).href);
 assert(await page.locator('img').evaluateAll(images=>images.every(i=>i.complete&&i.naturalWidth>0)));
 assert((await page.locator('main').innerText()).includes('第一代 AI'));
 await page.goto(pathToFileURL(path.join(root,'people/ash.html')).href);
 assert((await page.locator('main').innerText()).includes('本人不知'));
 assert((await page.locator('main').innerText()).includes('不会长大'));
 assert((await page.locator('.future-design').innerText()).includes('青年和中年'));
 await page.goto(pathToFileURL(path.join(root,'people/xerneas.html')).href);
 assert((await page.locator('.future-design').innerText()).includes('仅作为执行者候选'));
 assert.equal(await page.locator('.event').count(),0);
 await page.goto(pathToFileURL(path.join(root,'timeline.html')).href);
 const timelineText=await page.locator('main').innerText();
 assert(timelineText.includes('具体间隔未定')&&timelineText.includes('仅大木博士知晓'));
 assert(!timelineText.includes('T=0 对应本作 1967 年'));
 assert(!timelineText.includes('1975-09-01')&&!timelineText.includes('1974-04-01'));
 assert((await page.locator('#viridian-center time').innerText()).includes('未定'));
 assert.equal(await page.locator('#first-world-tournament').count(),0);
 assert((await page.locator('#unwritten-old-kanto-government-fall').innerText()).includes('怎样垮台尚未创作'));
 assert((await page.locator('#unwritten-oak-giovanni-evolution').innerText()).includes('不预定背叛或决裂'));
 const future=page.locator('#future-ash-human-body');
 assert((await future.innerText()).includes('执行者未定'));
 assert((await future.innerText()).includes('首选建议：哲尔尼亚斯'));
 assert((await future.innerText()).includes('少年、青年和中年'));
 await future.screenshot({path:path.join(root,'ash-human-body-direction.png')});
 await page.setViewportSize({width:1400,height:1000});await page.screenshot({path:path.join(root,'worldline-opening.png')});
 assert.equal(await page.locator('.event').count(),23);
 await page.setViewportSize({width:390,height:900});await page.screenshot({path:path.join(root,'timeline-mobile.png'),fullPage:true});
 assert(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth+1));
 await page.keyboard.press('Control+Home');await page.keyboard.press('Tab');assert(await page.locator(':focus').count()===1);
 await page.addStyleTag({content:'html{font-size:200%}body{font-size:24px}'});assert(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth+1));
 await page.goto(pathToFileURL(path.join(root,'rivals.html')).href);
 assert.equal(await page.locator('.rival-card').count(),18);
 assert((await page.locator('#entry-giovanni').innerText()).includes('报名受阻'));
 assert((await page.locator('#rival-silver').innerText()).includes('父子'));
 assert((await page.locator('#rival-giselle').innerText()).includes('非官方资助关系'));
 assert(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth+1));
 await page.setViewportSize({width:1400,height:1000});await page.screenshot({path:path.join(root,'rival-casting.png')});
 await page.locator('#rival-silver').getByRole('link',{name:'小银',exact:true}).click();
 assert((await page.locator('.source-identity').innerText()).includes('父子'));
 assert((await page.locator('.rival-proposal').innerText()).includes('是否接受支持'));
 assert.equal(await page.locator('.event').count(),0);
 await page.setViewportSize({width:390,height:900});assert(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth+1));
 assert.deepEqual(errors,[]);console.log('PASS: 10 maps, 79-person search and 38 candidates, 18 casting records, separate source facts/proposals/biography, legal-status barrier, future direction, keyboard and narrow layouts');
}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
