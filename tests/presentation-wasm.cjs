const fs=require('node:fs'),assert=require('node:assert/strict');
(async()=>{const {instance}=await WebAssembly.instantiate(fs.readFileSync('build/pallet/test_presentation.wasm'));const x=instance.exports;assert(x.presentation_test(),`Presentation assertion at C line ${x.presentation_failure_line()}`);console.log('PASS: portable presentation clock wrap, skip pause, auto advance, resume and simulation/audio independence');})().catch(e=>{console.error(e);process.exitCode=1;});
