# 本轮验证记录

日期：2026-09-17。范围为研究交付和项目骨架。

- `tools/validate_scaffold.ps1` 通过：5 个配置/内容 JSON 可解析；14 条地区 ID 唯一；公平设计不变量保持；30 个下载文件 SHA-256 匹配。
- 自编 Markdown 的相对文件链接全部可解析；未要求下载的上游文档引用在本地都存在。
- 9 个 PNG 均通过签名检查；尺寸为战斗正背面 64×64、图标素材 32×64。详细结果见 `sample-image-metadata.json`。
- 样本 PNG 均为索引色（PNG color type 3），文件编码位深包含 4 和 8；PNG 编码位深不等于最终 GBA 运行时图像格式，需经上游图像编译器和色板数量校验后判断。
- 15 个美术文件 = 9 个 PNG + 6 个普通/异色色板；15 个证据文件来自同一固定提交。
- 源码复核：`AI_FLAG_SMART_TRAINER` 组合包含全知标志，已记入公平 AI 必改项。

初次研究阶段未完成：完整引擎 checkout、编译器安装、ROM 构建、上游测试、游戏流程测试、公平 AI 实现、平衡对局、真机验证。后续编译器和核心测试进展见下文；上述骨架校验不为 ROM 和游戏流程提供通过结论。

## 跨平台架构修订后的检查

- `config/project.json` 升至 schema version 2：项目目标为 portable_game_core；gba_hardware 为首发目标，32 MiB 预算移至该平台；无画面宿主必做，未来 3D 引擎未选择。
- 更新后的校验器通过 7 个 JSON、14 个地区 ID、跨平台/公平设计不变量、同一任务的 GBA 与 3D 两组必需锚点覆盖、30 个已下载文件哈希。
- 修订后的自编 Markdown 本地链接检查通过。
- 新任务/绑定文件是契约示例，没有对应运行时解释器或真实场景；没有声称主机/GBA 任务或战斗一致性已实测通过。

## 战斗政策实现后的检查

- 工具链：项目缓存安装 Zig 0.13.0，无系统级安装；开发机参考库固定 pokemon-showdown 0.11.11，pnpm 锁文件保存依赖完整性。
- `tools/test_core.ps1` 通过 60,392 项检查，其中等级矩阵 3×100×100 输入各检查返回码与结果。边界检查包括来源组合、EV/IV、捕捉门槛、仓库满不修改、实例冲突、机制独立次数与无限重赛入口。
- 同一 C99 核心通过 ARM7TDMI/Thumb freestanding 对象编译；这是对象文件，不是已链接 ROM 或真机结果。
- `node tools/legality-reference/test.cjs` 通过 12 项真实参考校验，包含合法喷火龙与非法神速、特性、EV/IV、等级及赛制禁用检查。
- 更新骨架检查通过 9 个配置/示例 JSON、羁绊名单、14 地区、三档难度、独立额度、双平台锚点和 30 文件哈希。
- 核心目录使用合成数据测试；全图鉴生产目录、剧情捕捉门槛表、完整合法路径生成、ROM 存储适配及 AI 尚未完成。

## 图鉴运行时实现

- `tools/build_pokedex.ps1`：从本地哈希校验 CSV 生成中文名，再生成 2,514 条档案索引、参考属性/种族值/特性、进化和招式来源；1,025 基础物种中文名齐全。C 图鉴原生测试通过，核心及完整索引均编译为 ARM7TDMI 对象，同源核心亦编译为浏览器 Wasm。
- `tests/pokedex-wasm.cjs`：全部条目 ID 与 C 表一致；所有分类和属性筛选对照通过；中文/英文搜索、独立 Mega 登记、研究条目保护、损坏保存不修改状态通过。
- `tests/pokedex-preview.cjs`：Edge 实际渲染 1440/820/390/320 宽度；搜索、分页布局、登记刷新、错误导入、只读候选、无结果、键盘焦点、图像失败、资料失败重试通过，无页面脚本异常。
- 已查看桌面与手机截图。未进行真人屏幕阅读器测试、真机测试或完整 WCAG 认证；这些不以自动化通过替代。
- 浏览器预览可查看数据，但游戏内 GBA 界面、中文字体/图片打包、捕捉剧情和全部特殊形态内容核实仍待接入。实际范围见 `docs/12-pokedex-implementation.md`。
