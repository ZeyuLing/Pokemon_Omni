const query=document.querySelector('#query');
if(query){const status=document.querySelector('#status'),rows=[...document.querySelectorAll('.person-link')],count=document.querySelector('#count'),empty=document.querySelector('#empty');function filter(){let total=0;const term=query.value.trim().toLocaleLowerCase();for(const row of rows){const show=row.textContent.toLocaleLowerCase().includes(term)&&(!status.value||row.dataset.status===status.value);row.hidden=!show;if(show)total++;}count.textContent=`显示 ${total} / ${rows.length} 位角色`;empty.hidden=total!==0;}query.addEventListener('input',filter);status.addEventListener('change',filter);filter();}
